# keyword agruments = an argument preceded by an identitfer
#                     helps with readability
#                     order of arugments doesn't matter
# 4 types:            1. positional 2. default 3. KEYWORD (the one covered in this file) 4. arbitrary

def hello(greeting, title, first, last):
    print(f"{greeting} {title}{first} {last}")

hello("Hello", "Mr.", "Jun", "Ma")
hello("Hello", title="Mr.", last="Ma", first="Jun") #Beware that positional arguments precede keyword arguments

for x in range(1,11):
    print(x, end=" ") #end= is a keyword argument that replaces each end line with a space allowing you to keep everything within one line

print("1","2","3","4","5", sep=" ")

def get_phone(country, area, first, last):
    return f"{country}--{area}--{first}--{last}"

phone_num = get_phone(country=+1, area=604, first= 123, last= 456)

print(phone_num)